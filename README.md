# netsim-lab
A Python framework for simulating computer networks — TCP/IP, ICMP ping, routing algorithms (Dijkstra), and topologies (Star, Mesh, Ring, Bus). Includes node failure/recovery simulation and pytest test suite.
